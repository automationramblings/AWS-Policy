poweruser:
  config:
    Policy:
      - "Lambda-CICD"
      -  "S3-DEVOPS" 
    Service:
      - "lambda.amazonaws.com"
      - "s3.amazonaws.com"
      - "ec2.amazonaws.com"

devops:
  config:
    Policy:
      -  "S3-DEVOPS"
    Service:
      - "s3.amazonaws.com"
      - "ec2.amazonaws.com"
cicd:
  config:
    Policy:
      - "Lambda-CICD"
    Service:
      - "lambda.amazonaws.com"

ec2admin:
  config:
    Policy:
      - "EC2Admin"
    Service:
      - "ec2.amazonaws.com"

lambda-devops:
  config:
    Policy:
      - "Lambda-CICD"
    Service:
      - "lambda.amazonaws.com"


ec2admin-devops:
  config:
    Policy:
      - "S3-DEVOPS"
      - "EC2Admin"
    Service:
      - "ec2.amazonaws.com"